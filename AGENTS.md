# Agent Instructions

The canonical project instructions are in [`AGENT.md`](AGENT.md). Read and follow that file in full before making changes. More specific `.cursor/rules/*.mdc` files apply to matching paths.
